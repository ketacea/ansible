---
- name: supervisor manager
  hosts: supervisor
  user: keta
  tasks:
    - name: supervisor
      include_role:
        name: supervisor
      tags:
        - install
        - config
        - restart
        - start
        - stop
        - status
        - uninstall
